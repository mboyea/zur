#include "Sprite.h"

Sprite::Sprite(Texture texture, bool overwriteBackground, Int2D position, Pivot pivot)
	: texture(texture), position(position), overwriteBackground(overwriteBackground), pivot(pivot) {
	//
}
